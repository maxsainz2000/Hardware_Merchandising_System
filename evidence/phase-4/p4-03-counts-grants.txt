P4-03 - db/grants/0012_counts-and-adjustments-grants.sql - grants evidence
Captured 2026-08-29, against the real pinned MariaDB instance (ADR-002:
10.4.32-MariaDB via XAMPP 8.2.12-0).

Schema evidence is a separate file: p4-03-counts-schema.txt.

merch_api's password is not known to this shell (it lives in
%ProgramData%\MerchandisingSystem\config\database.json, ADR-013), so the
manual mysql-CLI verification this file would otherwise run directly is
performed instead through ConnectionFactory/DatabaseOptionsLoader, which
reads that same config - exactly the identity the running API uses. That is
StockCountSchemaTests, and its relevant console output is reproduced below
rather than a second, credential-guessing transcript.


===============================================================================
1. Apply 0012_counts-and-adjustments-grants.sql as root, after migration 0010
===============================================================================

$ mysql -u root merchandising < db/grants/0012_counts-and-adjustments-grants.sql
(no output - clean apply)

$ mysql -u root -e "SHOW GRANTS FOR 'merch_api'@'localhost';" | grep -E "stockcount|stockadjustment"
GRANT INSERT ON `merchandising`.`stockcountlines` TO `merch_api`@`localhost`
GRANT INSERT, UPDATE ON `merchandising`.`stockadjustments` TO `merch_api`@`localhost`
GRANT INSERT, UPDATE ON `merchandising`.`stockcounts` TO `merch_api`@`localhost`

  [PASS] table names lowercase (@@lower_case_table_names = 1, ADR-003.1).
  [PASS] stockcountlines: INSERT only - recorded once, at count time, never
         edited afterward (the migration's own "what was true then" argument).
  [PASS] stockcounts, stockadjustments: INSERT, UPDATE - the two headers
         whose Status changes in place (Open/Closed/Approved/Rejected and
         Pending/Approved/Rejected/Applied respectively).
  [PASS] nothing added for stockmovements or auditlogs - unaffected by this
         file, ADR-013 untouched.


===============================================================================
2. Behaviour - the grant boundary, proven as merch_api (StockCountSchemaTests)
===============================================================================

$ dotnet test ... --filter "FullyQualifiedName~StockCountSchemaTests"

  Passed MerchApi_CanInsertAndUpdateHeaders_AndInsertOnlyLines [29 ms]
  Passed MerchApi_HasNoDeleteGrant_OnAnyOfTheThreeTables [25 ms]
  Passed MerchApi_HasNoUpdateGrant_OnStockCountLines [19 ms]
  Passed MerchApi_CannotAlterOrDropTheNewTables [22 ms]

  [PASS] merch_api, using the API's own configured credential, can INSERT a
         stock count, a count line and an adjustment, and can UPDATE
         StockCounts.Status (+ RowVersion) and StockAdjustments.Status
         (+ ApprovedByUserId, RowVersion) - proving the grant is sufficient
         for the shape P4-09 and P4-10 will need.

  [PASS] merch_api is refused DELETE on all three tables (ERROR 1142) -
         StockCounts, StockCountLines, StockAdjustments.

  [PASS] merch_api is refused UPDATE on StockCountLines, the one table this
         file does not grant it on, ERROR 1142.

  [PASS] merch_api holds no DDL on either of the two tables tried (ALTER on
         StockCounts, DROP on StockAdjustments) - both ERROR 1142. ADR-013's
         split is not weakened by adding three tables to it.


===============================================================================
3. What is deliberately not granted, and why (from the file's own header)
===============================================================================

DELETE, on any of the three, to anyone - spec section 12: "Transactional
records are never physically deleted."

UPDATE on stockcountlines - nothing in this phase's cards edits a count line
after it is written. If a later card needs one, that grant is argued in a
new numbered file, not added here by assumption.
